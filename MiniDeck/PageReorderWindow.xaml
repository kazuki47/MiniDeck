<Window x:Class="MiniDeck.PageReorderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ページの並べ替え"
        Width="430" Height="520"
        MinWidth="360" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        Background="#FF333333"
        Foreground="White">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#50808080"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#70A0A0A0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#90B0B0B0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PageListItemStyle" TargetType="ListBoxItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="ページをドラッグ＆ドロップして並べ替えます。"
                       FontSize="15" FontWeight="Bold"/>
            <TextBlock Text="「OK」を押すと設定画面の編集内容へ反映されます。"
                       Margin="0,5,0,0" FontSize="11" Opacity="0.75"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#40303030" BorderBrush="#80FFFFFF"
                BorderThickness="1" CornerRadius="5" Padding="10">
            <ListBox x:Name="PageList"
                     ItemsSource="{Binding PageOrder}"
                     ItemContainerStyle="{StaticResource PageListItemStyle}"
                     Background="Transparent"
                     BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     SelectionMode="Single">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border AllowDrop="True"
                                Cursor="SizeNS"
                                MinHeight="52"
                                Padding="12,8"
                                BorderBrush="#90FFFFFF"
                                BorderThickness="1"
                                CornerRadius="5"
                                PreviewMouseLeftButtonDown="PageItem_PreviewMouseLeftButtonDown"
                                PreviewMouseMove="PageItem_PreviewMouseMove"
                                PreviewDragOver="PageItem_PreviewDragOver"
                                Drop="PageItem_Drop">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="#30404040"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#50606060"/>
                                            <Setter Property="BorderBrush" Value="#FF80B8E8"/>
                                        </Trigger>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Background" Value="#405CA9E6"/>
                                            <Setter Property="BorderBrush" Value="White"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="↕" Foreground="White" FontSize="22" Margin="0,0,12,0" Opacity="0.75"/>
                                <TextBlock Grid.Column="1" Text="{Binding Name}" Foreground="White" FontSize="14" FontWeight="SemiBold"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Column="2" Text="ドラッグして移動" Foreground="White" FontSize="10" Opacity="0.65" Margin="12,0,0,0"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="84" Margin="0,0,10,0" IsDefault="True" Click="OK_Click"/>
            <Button Content="キャンセル" Width="84" IsCancel="True" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
